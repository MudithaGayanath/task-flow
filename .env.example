# Copy this file to .env for local development.
# On Railway, set DATABASE_URL in the service's Variables tab instead —
# Railway's MySQL plugin gives you this connection string automatically.
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"
